import React from "react";

const Dog = (props) => {
    return (
        <div className="child">
            <img style={{width: 300, heig
            : 300 }} src={props.url} alt="Dog api" />
        </div>
    )
}

export default Dog;